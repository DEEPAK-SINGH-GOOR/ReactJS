// No need for this file anymore
// import React from "react";

// const StyledComponent = ({ isStyled }) => {
//   return (
//     <div className={`styled-box ${isStyled ? "styled" : ""}`}>
//     </div>
//   );
// };

// export default StyledComponent;
